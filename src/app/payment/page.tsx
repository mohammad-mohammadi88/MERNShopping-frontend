import type { Metadata, NextPage } from "next";

export const metadata: Metadata = {
    title: "Payment Page",
    description: "",
};

const Payment: NextPage = async () => {
    return <div>hello</div>;
};

export default Payment;
